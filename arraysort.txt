#include<stdio.h>
#include<conio.h>
int main()
{
    int a[10],b[10],c[10],n1,n2,n3,i,j,temp,k;
    printf("Enter the size of first array:");
    scanf("%d",&n1);
    printf("Enter the array elements:\n");
    for(int i=0;i<n1;i++)
        scanf("%d",&a[i]);
    printf("The sorted array is: ");
    for(int i=0;i<n1;++i)
    {
      for(j=i+1;j<n1;++j)
      {
          if(a[i]>a[j])
          {
              temp=a[i];
              a[i]=a[j];
              a[j]=temp;
          }
      }
    }
      for(i=0;i<n1;++i)
      {
          printf("%d\t",a[i]);
      }
    printf("\nEnter the size of second array:");
    scanf("%d",&n2);
    printf("\nEnter the array elements:\n");
    for(int i=0;i<n2;i++)
        scanf("%d",&b[i]);
    printf("The sorted array is:");
    for(int i=0;i<n2;i++)
    {
      for(j=i+1;j<n2;j++)
      {
          if(b[i]>b[j])
          {
              temp=b[i];
              b[i]=b[j];
              b[j]=temp;
          }
      }
    }
      for(i=0;i<n2;++i)
      {
          printf("%d\t",b[i]);
      }

 n3=n1+n2;
 i=0,j=0,k=0;
while(i<n1 && j<n2)
{
    if(a[i]<b[j])
        c[k++]=a[i++];
        c[k++]=b[j++];

}
while(i<n1)
    c[k++]=a[j++];

    while(j<n2)
        c[k++]=b[j++];
    printf("\nThe merged array is\n",c);
    for(i=0;i<n3;i++)
    {
        printf("%d\t",c[i]);
    }

}